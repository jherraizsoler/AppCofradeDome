<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppCofradiaCliente" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
       <!--  <item name="android:textColorPrimary">@color/white</item> --> <!-- Color del título -->
    </style>

    <style name="appToolbar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="android:logo">@mipmap/ic_launcher_round</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowMenuButtonStyle</item>
    </style>

    <style name="OverflowMenuButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/white</item>
    </style>
    <style name="Theme.AppCofradiaCliente" parent="Base.Theme.AppCofradiaCliente" />
</resources>